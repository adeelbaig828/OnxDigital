import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    flex: 1,
    flexDirection: 'row',
    marginStart: 35,
  },
  itemName: {
    fontSize: 12,
    color: '#929292',
    flex: 0.5,
  },
  ratingText: {
    flex: 0.4,
    fontSize: 12,
    color: '#000',
    textAlign: 'right',
  },
});
